version: 2

jobs:
  pre:
    docker:
      - image: circleci/python:3.6
    steps:
      - run:
          command: |
            mkdir bla
            echo pre > ${CIRCLE_JOB}.txt
      - persist_to_workspace:
          root: "~"
          paths:
            - bla
  test:
    docker:
      - image: circleci/python:3.6
    steps:
      - attach_workspace:
        at: ~
      - run:
          command: |
            mkdir bla
            echo pre > ${CIRCLE_JOB}.txt
            ls -la bla
      - persist_to_workspace:
          root: "~"
          paths:
            - bla

  finalize:
    docker:
      - image: circleci/python:3.6
    steps:
      - attach_workspace:
        at: ~
      - run:
          command: |
            ls -la bla

workflows:
  version: 2
  bla:
    jobs:
      - pre
      - test:
          name: test-a
          requires:
            - pre
      - test:
          name: test-b
          requires:
            - pre
      - finalize:
          requires:
            - test-a
            - test-b
